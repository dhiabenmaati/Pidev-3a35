{% extends 'BaseBackOffice.html.twig' %}

{% block content %}
	<div style="margin: auto; width: 50%; border: 2px solid; padding: 10px;">
				<table class="table">
					<tbody>
					<h3 style="text-align: center">Livreur Information</h3>
					<br/><br/>
					</tbody>
						<tr>
							<th>Email</th>
							<td>{{ app.user.email }}</td>
						</tr>
						<tr>
							<th>Name</th>
							<td>{{ app.user.name }}</td>
						</tr>
						<tr>
							<th>Surname</th>
							<td>{{ app.user.surname }}</td>
						</tr>
						<tr>
							<th>Image</th>
							<td><img src="{{asset('uploads/images/'~ app.user.image)}}" height="50"></td>
						</tr>


					</tbody>
				</table>
		<!--		<div style="text-align: center">
				<a href="{{ path('user_edit', {'id': app.user.id}) }}">
					<button class="btn py-3 px-2 btn-primary">
						Edit Profile</button>
				</a>
			</div>  -->


                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
	</div>
<!-- End Shop Page -->
{% endblock %}
